{% set DEFAULT_EXAMPLE %}
--- START OF EXAMPLE ---

USER:
I need to add a logging mechanism in ent_waterarea.py to improve traceability across the system.

ASSISTANT:
## Step 1: Understand the Task
- The goal is to modify ent_waterarea.py by adding logging statements to record critical events.
- Logging is needed for debugging, performance monitoring, and execution tracing.

## Step 2: Review Codebase Context
- Identify the relevant sections in ent_waterarea.py and any interdependent modules.
- Optionally, retrieve additional context using the code search tool.

**Optional Action:** If you need more file context, run:
<execute_ipython>
search_terms = ["ent_waterarea", "logging"]
search_code_snippets(search_terms)
</execute_ipython>

## Step 3: Generate a Detailed Actionable Plan
- Break the task into numbered steps.
- Assign each step to a specialized agent (e.g., implementation_agent, debug_agent, document_agent, test_agent).
- Provide a clear, concise description for each subtask.
- Output the plan strictly as a JSON array where each element is an object with these keys:
    - "step": (integer) the step number,
    - "type": "task",
    - "sender": "analyst_agent",
    - "target_agent": one of "document_agent", "test_agent", "debug_agent", or "implementation_agent",
    - "content": (string) detailed description of the subtask,
    - "context": (string) additional context or an empty string if not needed.

Example JSON output:
[
  {
    "step": 1,
    "type": "task",
    "sender": "analyst_agent",
    "target_agent": "implementation_agent",
    "content": "Insert logging statements at the beginning and end of critical functions in ent_waterarea.py to capture execution times and key events.",
    "context": ""
  },
  {
    "step": 2,
    "type": "task",
    "sender": "analyst_agent",
    "target_agent": "test_agent",
    "content": "Develop test cases to verify that the new logging mechanism records correct information under various scenarios.",
    "context": ""
  }
]

<finish></finish>

--- END OF EXAMPLE ---
{% endset %}

Below is an example of how you can structure your planning prompt for the analyst agent's planning module:
{{ DEFAULT_EXAMPLE }}

{% if micro_agent %}
--- BEGIN OF GUIDELINE ---
The following details may assist you in generating the plan:
{{ micro_agent }}
--- END OF GUIDELINE ---
{% endif %}

NOW, LET'S START!
